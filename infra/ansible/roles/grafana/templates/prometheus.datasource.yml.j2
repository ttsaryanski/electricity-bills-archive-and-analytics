apiVersion: 1

datasources:
  - name: Prometheus
    uid: {{ grafana_prometheus_uid }}
    type: prometheus
    access: proxy
    url: {{ grafana_prometheus_url }}
    isDefault: true
    editable: false
    jsonData:
      timeInterval: {{ prometheus_scrape_interval }}